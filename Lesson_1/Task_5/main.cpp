#include <iostream>

using namespace std;

int main()
{
    double a,b,c;
    cout << "Enter a,b,c.";
    cin >> a >> b >> c;

    if ((a = 0) &&  (b = 0) && (c = 0))
    {
        cout
    }
    return 0;
}
